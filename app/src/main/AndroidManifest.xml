<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tr.edu.yildiz.selin_16011068">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Selin16011068">
        <activity android:name=".SoruGuncellemeActivity"></activity>
        <activity android:name=".SinavAyarlariActivity" />
        <activity android:name=".SinavOlusturmaActivity" />
        <activity android:name=".SoruListelemeActivity" />
        <activity android:name=".SoruEklemeActivity" />
        <activity android:name=".UyeOlmaActivity" />
        <activity android:name=".AnaEkranActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>